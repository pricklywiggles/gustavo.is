# Dependencies
node_modules/
.pnp/
.pnp.*
.venv/
venv/
__pycache__/

# Build output
dist/
build/
out/
.next/
.astro/
.svelte-kit/
.output/
*.tsbuildinfo
next-env.d.ts

# Vercel
.vercel

# fumadocs-mdx generated output — regenerated on next dev/build
.source

# impeccable local state (assets/ and design.json are committed)
.impeccable/hook.cache.json
.impeccable/config.local.json
.impeccable/live/

# Caches
.cache/
.turbo/
.parcel-cache/
.eslintcache

# Secrets and local env
.env
.env.*
!.env.example

# Editor and OS
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Test/coverage output
coverage/
.nyc_output/

*storybook.log
storybook-static

# Video source material: only the encoded scene_home.webm and scene_home.mov ship
public/input.mov
public/frames/

# Untracked folders
/tmp
# Licensed faces: tracked only as src/fonts/fonts.tar.enc, decrypted by
# scripts/fonts.mjs before dev and build. In the clear they would be the
# redistribution both foundry EULAs forbid.
src/fonts/*.woff2
src/fonts/*.otf

# ship-it worktrees live under .claude but must never be committed
.claude/worktrees/

# Trial faces for the dev font lab stay local: demo licenses do not cover
# redistribution, and production never loads them.
src/fonts/try/

# Panorama masters: local only, regenerate runtime sets with scripts/scale-panorama-set.mjs
assets/panorama-masters/
